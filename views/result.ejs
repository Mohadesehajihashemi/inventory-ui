<!DOCTYPE html>
<html lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>نتیجه</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>داده‌ها دریافت شد!</h1>
    <div class="container">
        <p><strong>نام:</strong> <%= firstName %></p>
        <p><strong>نام خانوادگی:</strong> <%= lastName %></p>
        <p><strong>کد ملی:</strong> <%= nationalCode %></p>
        <p><strong>شماره تماس:</strong> <%= phone %></p>
        <% if (email) { %>
            <p><strong>ایمیل:</strong> <%= email %></p>
        <% } %>
        <% if (address) { %>
            <p><strong>آدرس:</strong> <%= address %></p>
        <% } %>
        <p><strong>تحصیلات:</strong> 
            <% if (education === 'diploma') { %> دیپلم
            <% } else if (education === 'associate') { %> کاردانی
            <% } else if (education === 'bachelor') { %> کارشناسی
            <% } else if (education === 'master') { %> کارشناسی ارشد
            <% } else if (education === 'phd') { %> دکتری
            <% } %>
        </p>
        <p><strong>وضعیت تأهل:</strong> <%= maritalStatus === 'single' ? 'مجرد' : 'متأهل' %></p>
        <p><strong>جنسیت:</strong> <%= gender === 'male' ? 'مرد' : 'زن' %></p>
        <% if (description) { %>
            <p><strong>توضیحات:</strong> <%= description %></p>
        <% } %>
        <div class="button-group">
            <a href="/" class="primary">بازگشت به فرم</a>
            <a href="/list" class="primary">مشاهده لیست افراد</a>
            <a href="/products" class="primary">مدیریت کالاها</a>
        </div>
    </div>
</body>
</html>